/*#include <stdio.h>

int main(){
    // Kamus
    int i, j, N;

    // Algoritma
    printf("Masukkan N: ");
    scanf("%d", &N);

    // Nested Looping | Ada loop di dalam loop
    for (i=0; i <= N; i++){
        for(j=i; j < N; j++){
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
*/
