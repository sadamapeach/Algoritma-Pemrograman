/*#include <stdio.h>
int main()
{
    // Kamus
    char j;
    int i;

    // Algoritma
    printf("Mengecek Harga Diskon");
    printf("\nMasukkan harga= ");
    scanf("%d",&i);

    printf("\nMasukkan kode diskon= ");
    scanf("%s",&j);

    if (i>200 && i<10000)
        if (j=='A')
            return (0.9*i);
        else if (j=='B')
            return (0.85*i);
        else if (j=='C')
            return (0.8*i);
        else
            printf("Kode yang anda masukkan salah");

	else
        printf("Anda tidak memenuhi range harga");

    return 0;
}
*/
