/*#include <stdio.h>
int main()
{
    // Kamus
    int g;
    int j;

    // Algoritma
    printf("Menghitung Upah Kerja");
    printf("\nMasukkan golongan= ");
    scanf("%d",&g);

    printf("Masukkan lama jam bekerja= ");
    scanf("%d",&j);

    if (g==1)
        if (j<40)
            return (j*1000);
        else
            return (j*1000) + ((j-40)*(1.5*j*1000));

    else if (g==2)
        if (j<40)
            return (j*1500);
        else
            return (j*1500) + ((j-40)*(1.5*j*1500));

    else if (g==3)
        if (j<40)
            return (j*2000);
        else
            return (j*2000) + ((j-40)*(1.5*j*2000));

    else if (g==4)
        if (j<40)
            return (j*2500);
        else
            return (j*2500) + ((j-40)*(1.5*j*2500));

	else
        printf("Golongan yang anda masukkan salah");

    return 0;
}
*/
