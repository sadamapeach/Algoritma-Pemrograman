/*#include <stdio.h>

int main(){
    // Kamus
    int N, i, arr[100];
    float sum;

    // Algoritma
    sum = 0;
    printf("Masukkan nilai N: ");
    scanf("%d", &N);

    for(i = 1; i <= N; i++){
        if(i % 2 == 0){
            arr[i] = -i;
        }
        else{
            arr[i] = i;
        }
    }

    for(i = 1; i <= N; i++){
        sum += arr[i];
    }

    printf("\nHasil: %0.1f",sum);
    return 0;
}
*/
