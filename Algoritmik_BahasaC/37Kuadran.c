/*#include <stdio.h>

int main(){
    // Kamus
    int x,y;

    // Algoritma
    printf("Menentukan Kuadran\n\n");

    printf("Masukkan elemen x: ");
    scanf("%d",&x);

    printf("Masukkan elemen y: ");
    scanf("%d",&y);

    if (x>0 && y>0)
        printf("\nP(%d,%d) berada pada kuadran I",x,y);
    else if (x<0 && y>0)
        printf("\nP(%d,%d) berada pada kuadran II",x,y);
    else if (x<0 && y<0)
        printf("\nP(%d,%d) berada pada kuadran III",x,y);
    else if (x>0 && y<0)
        printf("\nP(%d,%d) berada pad kuadran IV",x,y);

    return 0;
}
*/
