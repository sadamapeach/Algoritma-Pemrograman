/*#include <stdio.h>

int main(){
    // Kamus
    int i, N;
    float sum;

    // Algoritma
    printf("Menghitung Jumlah Deret\n");
    printf("Masukkan N: ");
    scanf("%d",&N);

    for(i = 1; i <= N; i++){
        if(i == 1) printf("%d",i);
        else printf(" + %d",i);
        sum += i;
    }

    printf(" = %0.1f",sum);

    return 0;
}
*/
