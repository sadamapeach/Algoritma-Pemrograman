/*#include <stdio.h>

int main(){
    // Kamus
    int i, j, N;

    // Algoritma
    printf("Menyusun karakter '*' dalam rumus deret Un = 0.5*N*(N+1)");
    printf("\nMasukkan N: ");
    scanf("%d", &N);

    // Nested Looping | Ada loop di dalam loop
    for(i=1; i<=N; i++){
        for(j=1; j <= 0.5*i*(i+1); j++){
            printf("*");
        }
    printf("\n");
    }

    return 0;
}
*/
