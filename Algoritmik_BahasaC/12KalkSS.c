/*#include <stdio.h>
int main()
{
    // Kamus
    char huruf;
    int x;
    int y;

    // Algoritma
    printf("Mengecek Kalkulator");
    printf("\nMasukkan nilai x = ");
    scanf("%d",&x);

    printf("Masukkan nilai y = ");
    scanf("%d",&y);

    printf("\nMasukkan kode operasi = ");
    scanf("%s", &huruf);

    if (huruf=='a')
        return x += y;
    else if (huruf=='b')
        return (x-=y);
    else if (huruf=='c')
        return (x*=y);
    else if (huruf=='d')
        return (x/=y);
    else if (huruf=='e')
        return (x%=y);
    else
        printf("Bukan pilihan menu yang benar");

    return 0;
}
*/
