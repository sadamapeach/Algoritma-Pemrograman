/*#include <stdio.h>

int main(){
    // Kamus
    int N, i;
    float sum;

    // Algoritma
    sum = 0;
    printf("Masukkan nilai N: ");
    scanf("%d", &N);

    for (i = 1; i <= N; i++){
        if (i % 2 == 0) sum -= i;
        else sum += i;
    }
    printf("\nHasil: %0.1f",sum);
    return 0;
}
*/
