/*#include <stdio.h>

int main(){
    // Kamus
    int i, j, k, N;

    // Algoritma
    printf("")
    printf("Masukkan N: ");
    scanf("%d", &N);

    // Nested Looping | Ada loop di dalam loop
    if (N<=0){
        printf("Masukan harus bilangan positif!");
    }
    else{
        for (i = N; i >= 1; i--){
            for(j=i; j < N; j++){
                printf(" ");
        }
            for(k = N-1; k >= (N-i); k--){
                printf("* ");
            }
            printf("\n");
        }
    }

    return 0;
}
*/
